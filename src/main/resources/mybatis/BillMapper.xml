<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.everygold.inf.etax.dao.BillDao">
  
  <select id="queryLoanList" resultType="java.util.Map">
  	SELECT * FROM loan_apply a ORDER BY a.LOAN_CREATE_DATE DESC
  </select>
  
</mapper>